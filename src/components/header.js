import React from 'react'

const Header = ({title}) => {
    return(

        <header className="header-container border">
            <h1>{title}</h1>
        </header>
    )
}

export default Header